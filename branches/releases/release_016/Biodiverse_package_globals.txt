my $a = {
   # these Biodiverse::GUI::Tabs::Labels defaults no longer used.  left as an example.
   #'Biodiverse::GUI::Tabs::Labels' => { 'CONVERT_BASEDATA_TO_MATRIX' => 1,  
   #                                     'CONVERT_TREE_TO_MATRIX' => 1,
   #                                     'CONVERT_BASEDATA_TO_TREE' => 1,
   #                                     'CONVERT_MATRIX_TO_TREE' => 1,
   #                                     }
};

#  to use this file, set an environment variable called
#  BIODIVERSE_DEFAULT_PARAMS to point to this file.
#  to keep it, but have it ignored for a run, set the variable
#  BIODIVERSE_DEFAULT_PARAMS_IGNORE to some true vaue (e.g. 1)
